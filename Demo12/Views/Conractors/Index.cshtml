@model IEnumerable<Demo12.Models.Conractor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContractorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConractorSupervisor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContractorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConractorSupervisor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CotractorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CotractorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CotractorID })
        </td>
    </tr>
}

</table>
